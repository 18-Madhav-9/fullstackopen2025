sequenceDiagram
    participant browser
    participant server

    Note right of browser: After clicking submitting button

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa with note data
    activate server
    server-->>browser: 201 Created (or success response)
    deactivate server

    Note right of browser: The browser's JavaScript updates the page dynamically with the new note (no reload)
    Note right of browser: Uses Ajax to directly updates the DOM
